# Constants

Folder ini memusatkan context key, role slug, permission slug, katalog pesan, dan `CustomError` API. Status HTTP dan kode error publik harus ditambahkan ke `custom_error.constant.go` agar handler tetap konsisten.
